[server]
port = 3333
host = "0.0.0.0"

[storage]
root = "."

[search]
engine = "sqlite"

[versioning]
strategy = "git"

[memory]
# Episodic files live here when memory_kind is unset (see docs/MEMORY.md).
# episodes_path_prefix = "episodes/"

[auth]
type = "none"

[backup]
remote = "https://github.com/XY01/kiwifs.git"
